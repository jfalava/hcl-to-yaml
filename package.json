{
  "name": "hcl-parser",
  "version": "0.1",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:windows-x64": "bun build --compile --minify --sourcemap --bytecode --target=bun-windows-x64-modern --define BUILD_VERSION='\"0.1\"' ./src/app.ts --outfile dist/generate-aws-inventory.exe && cd dist && zip generate-aws-inventory-windows-x64.zip generate-aws-inventory.exe",
    "build:linux-x64": "bun build --compile --minify --sourcemap --bytecode --target=bun-linux-x64-modern --define BUILD_VERSION='\"0.1\"' ./src/app.ts --outfile dist/generate-aws-inventory && cd dist && zip generate-aws-inventory-linux-x64.zip generate-aws-inventory",
    "build:macOS-arm": "bun build --compile --minify --sourcemap --bytecode --target=bun-darwin-x64 --define BUILD_VERSION='\"0.1\"' ./src/app.ts --outfile dist/generate-aws-inventory.app && cd dist && zip generate-aws-inventory-macos-arm.zip generate-aws-inventory.app",
    "build:all": "bun run build:windows-x64 && bun run build:linux-x64 && bun run build:macOS-arm",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "@types/js-yaml": "^4.0.9",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "js-yaml": "^4.1.0",
    "zod": "^4.1.12"
  }
}
